const styles = theme => ({
  '@global': {
    html: {
      width: '100%',
      height: '100%',
    },
    body: {
      width: '100%',
      height: '100%',
      backgroundColor: theme.palette.common.white,
    },
  },
});

export default styles;
